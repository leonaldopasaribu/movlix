import { Environment } from './environment.model';

const pageUrl = 'https://movlix-leonaldo-pasaribu.vercel.app';
const tmdbApiUrl = 'https://api.themoviedb.org';

export const environment: Environment = {
  pageUrl,
  tmdbApiUrl,
};
